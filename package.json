{
  "name": "composite-lambda-function",
  "version": "1.0.0",
  "description": "AWS lambda function to update composite datastore",
  "main": "index.mjs",
  "scripts": {
    "build": "npm install && zip -r build/lambda-deployment-package.zip . -x '*.git*' -x '.env' -x 'README.md'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.1",
    "ethers": "^5.7"
  }
}
