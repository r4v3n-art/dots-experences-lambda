{
  "name": "composite-lambda-function",
  "version": "1.0.0",
  "description": "AWS lambda function to update composite datastore",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.1",
    "ethers": "^5.7"
  }
}
